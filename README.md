# 1. Cấu trúc dự án

    - assets: image : hình ảnh app sử dụng, sass
    - component: sử dụng được nhiều lần các khối giao diện
    - context: truyền thông tin qua các lớp của component, không cần truyền dữ liệu qua các nhiều component
    - pages: các trang ở trang web, home, 404
    - parts:
    - services: lấy thông tin từ api
    - store: dùng vs redux ,
    - util: chứa các hàm sử dụng nhiều lần

# 2. Các công cụ sử dụng

# 3. Mock API

    - API là đường link URL, nằm ở backend,
    - FE cần dữu liệu hiển thị
    - API là sợi dây kết nối giữa BE-FE
